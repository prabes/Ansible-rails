---
- ping:

- name: Install required packages
  apt: name={{ item }} update_cache=yes
  loop:
    - curl
    - git-core  
    - tmux
    - htop
    - python3-pip
    - "{{ required_packages }}"
  tags: packages

  